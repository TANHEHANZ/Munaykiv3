export enum ROUTES {
  INITIAL = "/",
  BYID = "/:id",
  PREFICURL = "/api",
  UPLOAD = "/uploads",
  USER = PREFICURL + "/user",
  CONTACT = PREFICURL + "/contact",
  ALERT = PREFICURL + "/alert",
}
