export interface GoogleProfileDTO {
  email: string;
  name: string;
  providerId: string;
  providerType: string;
}
export interface FacebookProfileDTO {
  email: string;
  name: string;
  providerId: string;
  providerType: string;
}
